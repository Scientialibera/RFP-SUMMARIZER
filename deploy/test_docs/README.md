# Test Documents

Place test files here for `upload-test-files.ps1`:

- `capabilities/` - Company capabilities PDF (uploaded to the `reference` container)
- `rfp/` - Sample RFP PDFs (uploaded to `uploads` container to trigger processing)

These files are gitignored. Update paths in `deploy.config.toml` under `[test_docs]`.
